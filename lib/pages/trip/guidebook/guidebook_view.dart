import 'package:flutter/material.dart';

class GuidebookView extends StatelessWidget {
  const GuidebookView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Guidebook'));
  }
}
